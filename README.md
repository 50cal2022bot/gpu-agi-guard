# gpu-agi-guard
A lightweight AI‑driven daemon that auto‑switches your GPU between ultra‑low‑power eco mode and full performance when a target app (game, renderer, LLM) runs. It uses only native nvidia‑smi, works on Windows (extendable to Linux/AMD), and logs power, temperature and clocks to CSV.
